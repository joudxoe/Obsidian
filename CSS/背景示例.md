```
background-position:right top;			// 將圖像置於右上角
background-position:top right;			// 將圖像置於右上角
background-position:100% top;			// 將圖像置於右上角
background-position:right 0%;			// 將圖像置於右上角
background-position:0% right;			// 將圖像置於右上角
background-position:top 100%;			// 將圖像置於右上角
```

```
background-position:right bottom;		// 將圖像置於右下角
background-position:bottom right;		// 將圖像置於右下角
background-position:100% 100%;			// 將圖像置於右下角
background-position:100% bottom;		// 將圖像置於右下角
background-position:right 100%;			// 將圖像置於右下角
background-position:100% right;			// 將圖像置於右下角
background-position:bottom 100%;		// 將圖像置於右下角
```

```
background-position:top left;			// 將圖像置於左上角
background-position:left top;			// 將圖像置於左上角
background-position:0% 0%;				// 將圖像置於左上角
background-position:0% left;			// 將圖像置於左上角
background-position:top 0%				// 將圖像置於左上角
background-position:0% top;				// 將圖像置於左上角
background-position:left 0%;			// 將圖像置於左上角
```

```
background-position:left bottom;		// 將圖像置於左下角
background-position:bottom left;		// 將圖像置於左下角
background-position:0% bottom;			// 將圖像置於左下角
background-position:left 100%;			// 將圖像置於左下角
background-position:100% left;			// 將圖像置於左下角
background-position:bottom 0%;			// 將圖像置於左下角
```

```
background-position:center;				// 圖像居中
background-position:center center;		// 圖像居中
background-position:50% 50%;			// 圖像居中
```

```
background-position:50px 20px;		// 偏移點是圖像的左上角，向右50像素，向下20像素
```

```
background-color:#f0e68c;			// 設定背景顏色
```

```
background-image:url(leaf.jpg);		// 設定背景圖像
```

```
background-repeat:no-repeat;		// 不重覆背景圖
```

```
background-repeat:repeat-x;			// 水平重覆背景圖
```

```
background-attachment:fixed;		// 固定背景圖，不隨頁面滾動
```

```
background-attachment:scroll;		// 背景圖隨頁面滾動
```

```
background-size:100px 80px;			// 設定圖片的寬度為100像素，高度為80像素
```

```
background-size:100% 100%;			// 伸展背景圖完全填充內容區域
```

```
background-size:contain;			// 背景圖片按比例自適應內容區域
```

```
background:#fff url(main.png) no-repeat fixed center center;	// 背景簡寫屬性
```

多重背景
```
background-image:url(file1.png),url(file2.png);
background-position:left top,right bottom;
```

```
background:url(file1.png) left top,url(file2.png) right bottom;
```

全尺寸背景 (用圖片填滿整個頁面)
```
background:url(background.png) no-repeat center fixed;	// 背景色未指定，預設為透明
background-size:cover;
background-color: #eee;		// 覆寫背景色
```

透明漸變 - 淡入淡出
```
background:linear-gradient(to right, rgba(255,255,255,0), rgba(255,255,255,1)), url("image.jpg");
```