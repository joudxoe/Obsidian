```
$ git remote add <數據庫簡稱> <URL>					// 在本地端添加遠端數據庫
```

```
$ git remote add origin https://github.com/kaochenlong/dummy.git		// 新增遠端節點
```

```
$ git remote add origin git@github.com:kaochenlong/dummy.git			// 新增遠端節點
```

```
$ git remote
```

```
$ git remote -v															// 檢視遠端節點
```