一次讀取檔案全部內容
```
readfile('log.txt');
```

```
$file=file(log.txt);		// 以陣列的方式逐行存取檔案內容
foreach($file as $line){
	printf('%s<br>',$line);
}
```

```
$content=file_get_contents('index.php');
echo '<pre>'.htmlspecialchars($content).'</pre>';
```

```
$file='log.txt';
$fp=fopen($file,'rb');
echo fread($fp,filesize($file));
```

逐行讀取檔案內容
```
$fp=fopen('log.txt','rb');
while(!feof($fp)){
	printf('%s<br>',fgets($fp));
}
```

逐字讀取檔案內容
```
$fp=fopen('log.txt','rb');
while(!feof($fp)){
	echo fgetc($fp);
}
```

讀取圖片檔
```
header('Content-Type:image/gif');
$open=fopen($file,'rb');	// 以二進位模式讀取檔案
$image=fread($open,filesize($file)); // fread()可以讀取二進位檔(圖片、聲音、影像)
fclose($open);
echo $image;
```